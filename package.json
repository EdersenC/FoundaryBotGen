{
  "name": "foundry-npcbot",
  "version": "0.2.1",
  "private": true,
  "description": "Development and validation commands for the Foundry NPCBOT module and local companion.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "companion": "npm --prefix companion start",
    "test": "node --test",
    "validate": "node tools/validate-workspace.mjs",
    "check": "npm run test && npm run validate",
    "package": "node tools/package-module.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
